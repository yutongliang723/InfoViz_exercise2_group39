[project]
name = "infovis-ex2"
version = "0.1.0"
description = "Information Visualization - Exercise 2 (Group 39)"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.0",
    "pandas>=2.0",
    "scikit-learn>=1.4",
    "numpy>=1.26",
]

[project.scripts]
serve = "app:main"
